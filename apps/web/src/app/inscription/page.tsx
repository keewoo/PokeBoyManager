import { EmptyState } from "@/components/empty-state";

export default function InscriptionPage() {
  return (
    <EmptyState
      title="Inscription"
      description="Le formulaire de création de compte arrive avec le lot v1-auth."
    />
  );
}
